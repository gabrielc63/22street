<% provide(:title, 'All users') %>
<h1>All users</h1>
<ul class="users">
    <% @users.all.each do |user| %>
        <li>
            <%= image_tag user.avatar.url(:thumb), class: "avatar_post" %>
            <%= link_to user.name, user %>
        </li>
    <% end %>
</ul>
